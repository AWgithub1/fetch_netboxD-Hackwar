<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NetBox Devices</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
    </style>
</head>
<body>
    <h1>NetBox Devices</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>URL</th>
                <th>Name</th>
                <th>Device Type</th>
                <th>Sites</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for device in devices %}
            <tr>
                <td>{{ device.id }}</td>
                <td>{{ device.url }}</td>
                <td>{{ device.name }}</td>
                <td>{{ device.device_type.display }}</td>
                <td>{{ device.site.name }}</td>
                <td>{{ device.status.label }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
